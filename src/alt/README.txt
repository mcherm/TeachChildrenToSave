Thid directory contains files which are related to the application but
are NOT part of the application itself. For instance, it contains the
web content to display when the application is NOT deployed.