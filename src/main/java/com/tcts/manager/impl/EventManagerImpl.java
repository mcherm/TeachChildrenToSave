package com.tcts.manager.impl;

import com.tcts.manager.EventManager;
import com.tcts.model.Event;

public class EventManagerImpl implements EventManager{
	
 public boolean addEvent(Event event) {
	 //AWSConnectionManagerImpl connectionManager = new AWSConnectionManagerImpl();
	// result = connectionManager.addVolunteer(event);
	 //if (result == 0)
		 return true;
	 //else return false;
 }
}
